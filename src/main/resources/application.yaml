server:
  port: 8082
spring:
  application:
    name: core
  datasource:
    url: jdbc:mysql://localhost:3306/gymbrodb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: gymbro
    password: gymflow2025
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger
